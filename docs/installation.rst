============
Installation
============

At the command line::

    $ easy_install flip_them_all

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv flip_them_all
    $ pip install flip_them_all
